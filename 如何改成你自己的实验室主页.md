# 如何将动漫主题网站改成你自己实验室的主页

这个仓库已经被改造成了一个动漫主题的示例网站。下面是详细的修改指南，教你如何把它改成你自己实验室的真实主页。

## 📁 主要文件结构

```
IdkeIO/
├── _config.yaml          # 网站全局配置（标题、描述、链接等）
├── index.md              # 主页内容
├── _members/             # 成员信息目录
│   ├── zhenghao-liu.md   # 每个成员一个文件
│   ├── ge-yu.md
│   └── ...
├── _data/
│   ├── highlights.yaml   # 亮点论文列表
│   └── citations.yaml    # 完整论文列表
├── _posts/               # 新闻动态
│   ├── 2025-01-22-post.md
│   └── ...
├── images/
│   └── headshots/        # 成员头像图片
└── projects/             # 项目页面
```

## 🔧 修改步骤

### 1️⃣ 修改网站基本信息

编辑 `_config.yaml` 文件：

```yaml
# 第2-4行：修改标题和描述
title: 你的实验室名称
subtitle: Your Lab Name in English
description: '你的实验室简介...'

# 第11-15行：修改联系方式
links:
  email: your-email@your-university.edu.cn
  google-scholar: 你的GoogleScholar ID
  github: 你的GitHub用户名
  orcid: 你的ORCID号
```

### 2️⃣ 修改主页内容

编辑 `index.md` 文件：

- **第4-16行**：修改"研究所简介"部分，介绍你的实验室
- 保留下面的 `{% include ... %}` 语法（这是Jekyll模板语法，会自动加载论文、新闻等）

### 3️⃣ 修改成员信息

每个成员对应 `_members/` 目录下的一个 `.md` 文件。

**修改现有成员文件**（如 `_members/zhenghao-liu.md`）：

```yaml
---
name: 你的姓名（英文）
name_cn: 你的姓名（中文）
image: images/headshots/your-photo.jpg  # 头像路径
role: pi          # 角色：pi=负责人, co=合作者, phd=博士生, master=硕士生, graduated=已毕业
year: 2021        # 加入年份
link: https://your-homepage.com
affiliation: your-email@university.edu.cn
description: Professor  # 职位描述（可选）
---

这里写个人简介
```

**添加新成员**：
1. 复制一个现有的 `.md` 文件
2. 重命名为 `firstname-lastname.md`（小写，用横线连接）
3. 修改文件内容
4. 将头像图片放到 `images/headshots/` 目录

**删除不需要的成员**：
- 直接删除对应的 `.md` 文件即可

### 4️⃣ 修改论文列表

编辑 `_data/highlights.yaml`（亮点论文）或 `_data/citations.yaml`（完整列表）：

```yaml
- authors:
  - 作者1
  - 作者2
  - 作者3
  date: '2025-01-01'
  mark: conference2025-main  # 用于分类筛选
  image: images/projects/project-name.png  # 项目图片（可选）
  link: 'https://arxiv.org/pdf/...'
  publisher: 会议/期刊名称
  title: '论文标题'
```

**mark 字段说明**：用于在不同页面筛选显示
- `nips2025`, `iclr2025`, `emnlp2025-main` 等：在主页会根据这些标记分类显示

### 5️⃣ 添加新闻动态

在 `_posts/` 目录下创建新文件，命名格式：`YYYY-MM-DD-post.md`

```markdown
---
title: 新闻标题
date: 2025-01-22
---

新闻正文内容...

可以使用 Markdown 格式编写
```

### 6️⃣ 替换图片

1. **头像图片**：将成员照片放到 `images/headshots/` 目录
   - 建议尺寸：400x400 像素
   - 格式：JPG 或 PNG

2. **项目图片**：放到 `images/projects/` 目录
   - 用于论文列表展示

3. **背景图片**：
   - 修改 `_config.yaml` 第5-6行的 `header` 和 `footer` 路径

### 7️⃣ 修改其他页面

- `team/index.md` - 团队页面
- `research/index.md` - 研究方向页面
- `projects/index.md` - 项目页面
- `contact/index.md` - 联系方式页面
- `course/index.md` - 课程页面

每个页面都是 Markdown 文件，可以直接编辑内容。

## 🚀 本地预览

这个网站使用 Jekyll 构建。本地预览步骤：

```bash
# 1. 安装 Ruby 和 Jekyll（如果还没有）
# Ubuntu/Debian:
sudo apt-get install ruby-full build-essential zlib1g-dev
gem install jekyll bundler

# 2. 安装依赖
cd IdkeIO
bundle install

# 3. 启动本地服务器
bundle exec jekyll serve

# 4. 在浏览器打开 http://localhost:4000
```

## 📤 部署到 GitHub Pages

1. 将修改后的代码推送到 GitHub 仓库
2. 仓库名改为：`你的用户名.github.io`
3. 在 GitHub 仓库设置中启用 GitHub Pages
4. 网站会自动部署到 `https://你的用户名.github.io`

## 💡 注意事项

1. **文件命名**：成员文件名用小写+横线，如 `zhang-san.md`
2. **图片路径**：统一使用相对路径 `images/...`
3. **YAML 语法**：注意缩进（用空格，不要用Tab）
4. **日期格式**：统一使用 `YYYY-MM-DD` 格式
5. **中文支持**：确保文件使用 UTF-8 编码

## 🆘 常见问题

**Q: 修改后网站没有变化？**
A: 等待几分钟让 GitHub Pages 重新构建，或清除浏览器缓存

**Q: 图片显示不出来？**
A: 检查图片路径是否正确，确保图片已上传到仓库

**Q: 如何删除动漫相关内容？**
A:
- 删除 `_members/` 下所有动漫角色文件
- 修改 `_data/highlights.yaml` 和 `_data/citations.yaml`
- 修改 `_config.yaml` 和 `index.md` 的介绍文字
- 替换 `images/headshots/` 下的头像图片

**Q: 如何添加新的页面？**
A: 创建新的 `.md` 文件，参考现有页面的格式

## 📚 参考资源

- [Jekyll 官方文档](https://jekyllrb.com/)
- [Lab Website Template 文档](https://greene-lab.gitbook.io/lab-website-template-docs)
- [Markdown 语法指南](https://www.markdownguide.org/)

---

**重要提示**：目前网站使用的是动漫角色作为示例数据。在部署到正式环境前，请务必：
1. 替换所有成员信息为真实信息
2. 更新所有论文和项目信息
3. 替换所有图片为真实照片
4. 修改所有联系方式和链接

祝你顺利搭建实验室主页！🎉
