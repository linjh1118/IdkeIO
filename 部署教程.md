# GitHub Pages 部署教程

本教程将教你如何将这个 Jekyll 网站部署到 GitHub Pages。

## 📋 前提条件

- 已有 GitHub 账号
- 代码已推送到 GitHub 仓库：`https://github.com/linjh1118/IdkeIO`

## 🎯 两种部署方式

### 方式一：作为子路径部署（推荐，如果你已有个人主页）

如果你已经有个人 GitHub Pages（`linjh1118.github.io`），可以将这个项目部署为子路径。

**访问地址将是：** `https://linjh1118.github.io/IdkeIO/`

#### 步骤：

1. **进入仓库设置**
   - 打开 https://github.com/linjh1118/IdkeIO
   - 点击 `Settings`（设置）

2. **启用 GitHub Pages**
   - 在左侧菜单找到 `Pages`
   - 在 `Build and deployment` 部分：
     - Source: 选择 `GitHub Actions`
   - 保存设置

3. **修改配置文件**

   编辑 `_config.yaml`，添加 baseurl（如果还没有的话）：

   ```yaml
   # 在文件开头添加
   baseurl: "/IdkeIO"  # 注意：要和仓库名一致
   ```

4. **推送修改**

   ```bash
   git add _config.yaml
   git commit -m "配置 baseurl 用于 GitHub Pages 部署"
   git push
   ```

5. **等待部署完成**
   - 回到 GitHub 仓库页面
   - 点击上方的 `Actions` 标签
   - 查看部署进度（需要 2-5 分钟）
   - 部署成功后，访问：`https://linjh1118.github.io/IdkeIO/`

---

### 方式二：作为独立域名部署

如果你想让这个网站成为你的主页，或使用自定义域名。

#### 选项 A：作为主 GitHub Pages

1. **重命名仓库**
   - 进入仓库设置 `Settings`
   - 在 `Repository name` 将仓库名改为：`linjh1118.github.io`
   - 点击 `Rename`

2. **修改配置**

   编辑 `_config.yaml`：
   ```yaml
   baseurl: ""  # 清空 baseurl
   url: "https://linjh1118.github.io"
   ```

3. **启用 GitHub Pages**
   - `Settings` → `Pages`
   - Source: 选择 `GitHub Actions`

4. **推送并部署**
   ```bash
   git add _config.yaml
   git commit -m "配置为主 GitHub Pages"
   git push
   ```

5. **访问**
   - 网址：`https://linjh1118.github.io`

#### 选项 B：使用自定义域名

1. **先完成方式一或选项 A 的部署**

2. **配置自定义域名**
   - `Settings` → `Pages`
   - 在 `Custom domain` 输入你的域名（如 `lab.example.com`）
   - 点击 `Save`

3. **配置 DNS**

   在你的域名服务商处添加 DNS 记录：

   **如果是顶级域名（如 `example.com`）：**
   ```
   类型: A
   主机: @
   值: 185.199.108.153
   值: 185.199.109.153
   值: 185.199.110.153
   值: 185.199.111.153
   ```

   **如果是子域名（如 `lab.example.com`）：**
   ```
   类型: CNAME
   主机: lab
   值: linjh1118.github.io
   ```

4. **更新配置文件**

   编辑 `_config.yaml`：
   ```yaml
   url: "https://lab.example.com"
   baseurl: ""
   ```

5. **等待 DNS 生效**（通常需要几分钟到几小时）

---

## 🔧 部署配置说明

### GitHub Actions 工作流

我已经为你创建了 `.github/workflows/deploy.yml` 文件，它会：

1. 在每次推送到 `master` 分支时自动触发
2. 安装 Ruby 和依赖
3. 构建 Jekyll 网站
4. 部署到 GitHub Pages

### baseurl 配置

**重要概念：**

- **url**: 你的网站完整域名
- **baseurl**: 网站的子路径

**示例：**

| 部署方式 | url | baseurl | 最终地址 |
|---------|-----|---------|---------|
| 子路径 | `https://linjh1118.github.io` | `/IdkeIO` | `https://linjh1118.github.io/IdkeIO/` |
| 主页 | `https://linjh1118.github.io` | `` | `https://linjh1118.github.io/` |
| 自定义域名 | `https://lab.example.com` | `` | `https://lab.example.com/` |

## 🚨 常见问题

### Q1: 网站样式丢失？

**原因：** baseurl 配置不正确

**解决：**
- 如果部署为子路径，确保 `_config.yaml` 中有 `baseurl: "/IdkeIO"`
- 如果部署为主页，确保 `baseurl: ""`

### Q2: Actions 部署失败？

**检查：**
1. 确保 `Settings` → `Pages` → `Source` 选择了 `GitHub Actions`
2. 查看 Actions 标签下的错误日志
3. 确保 `Gemfile` 和 `Gemfile.lock` 存在

### Q3: 图片显示不出来？

**原因：** 图片路径问题

**解决：**
- 所有图片路径应使用相对路径，如：`images/headshots/xxx.jpg`
- 不要用绝对路径 `/images/...`

### Q4: 部署后显示 404？

**可能原因：**
1. DNS 还未生效（等待一段时间）
2. baseurl 配置错误
3. GitHub Pages 未启用

**检查：**
- 访问 `https://github.com/linjh1118/IdkeIO/settings/pages`
- 确认 Pages 已启用且状态为 "Your site is live"

### Q5: 已有个人主页，如何添加这个项目？

**推荐方式：** 使用子路径部署（方式一）

你的个人主页仓库（`linjh1118.github.io`）和这个项目（`IdkeIO`）可以同时存在：
- 个人主页：`https://linjh1118.github.io/`
- 实验室主页：`https://linjh1118.github.io/IdkeIO/`

两个网站互不干扰，你可以在个人主页添加链接指向实验室主页。

## 📝 快速检查清单

部署前检查：

- [ ] 代码已推送到 GitHub
- [ ] `.github/workflows/deploy.yml` 存在
- [ ] `_config.yaml` 中 baseurl 配置正确
- [ ] GitHub Pages 已在仓库设置中启用
- [ ] Source 设置为 `GitHub Actions`

部署后检查：

- [ ] Actions 标签显示部署成功（绿色勾）
- [ ] Pages 设置中显示网站地址
- [ ] 访问网站能正常显示
- [ ] 图片和样式正常加载

## 🎉 下一步

部署成功后，你可以：

1. **自定义内容**
   - 参考 `如何改成你自己的实验室主页.md`
   - 修改成员、论文、新闻等信息

2. **本地预览**
   ```bash
   bundle install
   bundle exec jekyll serve
   # 访问 http://localhost:4000
   ```

3. **更新网站**
   - 修改内容后，推送到 GitHub
   - GitHub Actions 会自动重新部署

## 📚 参考资源

- [GitHub Pages 官方文档](https://docs.github.com/en/pages)
- [Jekyll 官方文档](https://jekyllrb.com/)
- [自定义域名配置](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)

---

有问题？检查 GitHub Actions 的错误日志，或参考上面的常见问题部分。
